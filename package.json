{
  "name": "stock-strategy-tester",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack trading strategy backtester with a modular strategy engine.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "concurrently \"cross-env PORT=5002 npm run start --workspace backend\" \"npm run start --workspace frontend -- --port 5174 --strictPort\"",
    "dev": "npm run start",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspace frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
